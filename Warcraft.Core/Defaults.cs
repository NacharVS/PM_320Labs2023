namespace Warcraft.Core;

public enum MoveSpeed
{
    Slow = 200,
    Standard = 300,
    Fast = 400
}

// attack speed in ms
public enum AttackSpeed
{
    Slow = 1000,
    Standard = 500,
    Fast = 200
}

public enum Health
{
    Tank = 1200,
    Standard = 600,
    Puny = 400
}

public enum Damage
{
    Powerful = 80,
    Standard = 40,
    Weakling = 20
}

public enum Cost
{
    Expensive = 10,
    Standard = 5,
    Cheap = 2
}

public enum Level
{
    Hero = 25,
    Creep = 10
}

public enum AttackRange
{
    Sniper = 1200,
    Standard = 600,
    Close = 300
}

public enum ManaCount
{
    Wizard = 1000,
    Standard = 500,
    Warrior = 200
}